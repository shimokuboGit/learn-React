# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: degital-meishi-firebase-hosting
on:
  push:
    paths:
      - "degital-meishi-app/**"
  workflow_dispatch:
permissions:
  checks: write
  contents: read
  pull-requests: write
jobs:
  build_and_preview:
    runs-on: ubuntu-latest

    steps:
      - name: Set up
        uses: actions/checkout@v4

      - name: install dependencies
        run: |
          npm ci
          npm install -g firebase-tools
        working-directory: degital-meishi-app

      - name: test
        run: npm run test
        working-directory: degital-meishi-app

      - name: build
        run: npm run build
        working-directory: degital-meishi-app

      # 諦めた
      # - name: deploy
      #   uses: FirebaseExtended/action-hosting-deploy@v0
      #   with:
      #     repoToken: ${{ secrets.GITHUB_TOKEN }}
      #     firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_DEGITAL_MEISHI_APP }}
      #     projectId: degital-meishi-app
      #     entryPoint: degital-meishi-app

      - name: deploy
        run: firebase deploy --token ${{ secrets.FIREBASE_TOKEN_MEISHI }}
        working-directory: degital-meishi-app