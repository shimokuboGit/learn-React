# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: degital-meishi-firebase-hosting
on:
  push:
    paths:
      - "degital-meishi-app/**"
  workflow_dispatch:
permissions:
  checks: write
  contents: read
  pull-requests: write
jobs:
  build_and_preview:
    runs-on: ubuntu-latest
    steps:
      - name: Set up
        users: actions/checkout@v4

      - name: Install depenencies and Build
        run: |
          npm ci
          npm run build
        working-directory: degital-meishi-app

      - name: deploy
        users: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_DEGITAL_MEISHI_APP }}
          projectId: degital-meishi-app
