# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: degital-meishi-firebase-hosting
on:
  push:
    paths:
      - "degital-meishi-app/**"
  workflow_dispatch:
permissions:
  checks: write
  contents: read
  pull-requests: write
jobs:
  build_and_preview:
    runs-on: ubuntu-latest

    steps:
      - name: Set up
        uses: actions/checkout@v4

      - name: Install depenencies and Build
        run: |
          npm ci
          npm run build
        working-directory: degital-meishi-app

      # - name: deploy
      #   uses: FirebaseExtended/action-hosting-deploy@v0
      #   with:
      #     repoToken: ${{ secrets.GITHUB_TOKEN }}
      #     firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_DEGITAL_MEISHI_APP }}
      #     projectId: degital-meishi-app
      #     entryPoint: degital-meishi-app

      - name: firebase tool
        run: npm install -g firebase-tools
        working-directory: degital-meishi-app
      - name: deploy
        run: firebase deploy --token 1//0eeqGENQPRL_sCgYIARAAGA4SNwF-L9Ir-CxeWq-a3wxAGOA595y89nobYrQnfR5AX-OlBcwri0Q7jMZvoyKV8k8w3dstqjFpPAM
        working-directory: degital-meishi-app